syntax = "proto3";
package gossip;

message Message {
    // TODO: This probably should not be a string, but parsing to BigInt was causing problems so leaving it like this for now
    string timestamp = 1;
    string text = 2;
}
